
pluginManagement {
    includeBuild("../node_modules/@react-native/gradle-plugin")
}
 
plugins {
    id("com.facebook.react.settings")
}

// Optional: If you need to customize autolinking (rarely needed)
extensions.configure(com.facebook.react.ReactSettingsExtension) { ex ->
    ex.autolinkLibrariesFromCommand()
}
 
rootProject.name = "WeiplCheckoutExample"
 
include(":app")
include(":react-native-weipl-checkout")
project(":react-native-weipl-checkout").projectDir = new File(rootProject.projectDir, "../../android")
includeBuild('../node_modules/@react-native/gradle-plugin')
